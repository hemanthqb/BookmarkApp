# Bookmark App (Backbone,Node,Express,Mongo)

This is the basic app which demonstrates the backbone, express, mongo features.

## Running
Use the below command to run the app( http://localhost:3000 )

    node server 
